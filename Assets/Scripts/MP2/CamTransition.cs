using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CamTransition : MonoBehaviour
{
    //public float m_TransitionSpeed = 50.0f;
    //public Transform m_CurrentCamView;
    //public Transform[] m_CamViews;

    //private void Update()
    //{
    //    if (Input.GetKeyDown(KeyCode.Alpha1))
    //    {
    //        m_CurrentCamView = m_CamViews[0];
    //    }

    //    if (Input.GetKeyDown(KeyCode.Alpha2))
    //    {
    //        m_CurrentCamView = m_CamViews[1];
    //    }

    //}
    //private void LateUpdate()
    //{
    //    transform.position = Vector3.Lerp(transform.position, m_CurrentCamView.position, Time.deltaTime * m_TransitionSpeed);
    //    transform.rotation = Quaternion.Lerp(transform.rotation, m_CurrentCamView.rotation, Time.deltaTime * m_TransitionSpeed);
    //}
}
